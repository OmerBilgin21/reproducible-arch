\set QUIET on
\pset null 'NULL'
\set ON_ERROR_ROLLBACK on
\set AUTOCOMMIT off
\set is_interactive `test -t 0 && echo 'true' || echo 'false'`
\if :is_interactive
  \x on
  \set PROMPT1 ''
  \set PROMPT2 ''
  \pset border 2
  \pset footer on
  \timing on
\endif
\set QUIET off
